import { Button } from "@mui/material";

const Dashboard = () => {
  return (
    <div>
      <Button href="/users" variant="contained">
        Go to Users
      </Button>
    </div>
  );
};

export default Dashboard;
